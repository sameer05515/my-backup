# 1. Start kafka + redis (docker)  
# 2. Run consumer  
`node consumer.js`  

# 3. Run app  
`node app.js`


## note:
1. redis docker compose file is placed at '`redis-docker\docker-compose.yml`'
2. kafka docker compose file is placed at '`kafka\docker\docker-compose.yml`'