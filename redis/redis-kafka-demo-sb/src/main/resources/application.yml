spring:
  application:
   name: demo
  mongodb:
    uri: mongodb://localhost:27017/redis_kafka_demo
    database: redis_kafka_demo
  redis:
    data:
      host: localhost
      port: 6379

server:
  port: 8089
